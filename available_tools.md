# Available Tools

The package exposes read-only MCP tools grouped into:

- `list_vcenters`, `get_vcenter_inventory`
- Core vCenter inventory tools
- VM guest and hardware detail tools
- Tagging tools
- Appliance visibility tools

Run `vcenter-mcp` in an MCP-compatible client to inspect the full registered tool list.
